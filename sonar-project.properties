sonar.projectKey=CarlosBoulder_202304-w7chwe-Carlos-alvarez-back
sonar.organization=carlosboulder

# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8

# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
sonar.sources=./src

# Encoding of the source code. Default is default system encoding
sonar.sourceEncoding=UTF-8

sonar.test.inclusions=src/**/*.test.ts
sonar.coverage.exclusions=src/**/types.tsx, src/**/*.d.ts, src/loadEnvironment.ts, src/server/index.ts, src/index.ts, src/server/routers/robots/robotsRouter.ts, src/server/CustomError.ts

sonar.javascript.lcov.reportPaths=coverage/lcov.info
